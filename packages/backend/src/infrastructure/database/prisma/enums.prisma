enum SiteStatus {
  pending
  crawling
  active
  error
}

enum TooltipPosition {
  top
  bottom
  left
  right
}

enum AnalyticsEventType {
  walkthrough_started
  walkthrough_completed
  walkthrough_skipped
  chat_message
}

enum OAuthProvider {
  google
  facebook
  telegram
  github
  apple
}

enum UnansweredQuestionStatus {
  new
  contacted
  resolved
}

enum RatingType {
  positive
  negative
}
